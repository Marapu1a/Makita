# Копировать в parsers/.env перед запуском парсеров
# parsers/.env добавлен в .gitignore — не коммитить реальные значения

DB_HOST=localhost
DB_PORT=5432
DB_NAME=makita
DB_USER=postgres
DB_PASSWORD=your_password_here
